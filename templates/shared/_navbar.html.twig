<nav id="couleur" class="navbar navbar-expand-lg navbar-dark ">
	<link rel="stylesheet" href="{{ asset('css/navbar.css') }}">

	<div class="container-fluid">
		<img id="logo" src="{{asset('images/logo.jpg') }}" alt="logo" style="width:10%">

		<button class="navbar-toggler" type="button" style="color:black" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>

		<div class="collapse navbar-collapse" style="color:black" id="navbarColor01">
			<ul class="navbar-nav me-auto">


				<li class="nav-item">
					<a class="nav-link" href="{{ path('home') }}">
						<i class="fas fa-house-user"></i>
					</a>
				</li>


				<li class="nav-item">
					<a class="nav-link" href="{{ path('annonces_index') }}">Nos produits</a>
				</li>

				<li class="nav-item">
					<a class="nav-link" href="{{ path('contact') }}">
						Nous contacter
					</a>
				</li>
				{% if app.user is same as null %}
					<li class="nav-item">
						<a class="nav-link" href="{{ path('app_login') }}">
							mon compte
						</a>
					</li>
				{% endif %}

				{% if app.user %}

					<li class="nav-item">
						<a class="nav-link" href="{{ path('app_logout') }}">
							Se deconnecter
						</a>
					</li>

					{% if app.user.roles[0] is same as "ROLE_ADMIN" %}
						<li class="nav-item">
							<a class="nav-link" href="{{ path('admin_home') }}">
								<i class="fas fa-user-cog"></i>
							</a>
						</li>
					{% endif %}
				{% endif %}
			</ul>

		</div>
	</div>
</nav>
